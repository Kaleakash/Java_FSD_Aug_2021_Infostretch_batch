// In JS only function name match doesn't matter number 
//of parameter and type of parameter must be be match. 
function abc(a,b){
    console.log("Hello")
    console.log(a+" "+b)
}
abc(10,20);
abc("A","B");
abc(30);
abc();